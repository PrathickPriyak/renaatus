import type { Metadata } from "next";
import { CookiesPage, cookiesMetadata } from "@/components/marketing/legal-pages";

export const metadata: Metadata = cookiesMetadata;

export default CookiesPage;
