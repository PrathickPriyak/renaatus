# Application environment lives in website/.env.example.
# Copy that file to website/.env.local and fill in local (non-production) values.
#
# Never commit secrets. Never put production credentials in development.
